version: "3"
services: 
  nginx:
    image: nginx:alpine
    ports: 
      - "3001:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
  portfolyo-builder:
    build: .
    env_file:
      - ./.env